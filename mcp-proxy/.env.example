# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Vault server address (used by vault-mcp-server Docker container)
VAULT_ADDR=http://127.0.0.1:8200

# MCP proxy server port
PORT=3001

# Frontend origin for CORS (optional, defaults to http://localhost:5173)
FRONTEND_ORIGIN=http://localhost:5173

# Official HashiCorp Vault MCP Server Docker image
# VAULT_MCP_IMAGE=hashicorp/vault-mcp-server:latest
